"""Dashboard UI components (Plan 5 A1). Pure-ish render/helper modules that the
orchestrator (app/dashboard.py) composes. They consume posterior/CI data + the
shared palette (app/theme.py); they do not import the dashboard."""
